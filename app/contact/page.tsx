import { Section } from '@/components/Section';
import { Shell } from '@/components/Shell';

export default function ContactPage() {
  return (
    <Shell>
      <Section title="Contact" pathLabel="/contact">
        Placeholder route for future standalone contact page.
      </Section>
    </Shell>
  );
}
