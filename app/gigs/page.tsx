import { Section } from '@/components/Section';
import { Shell } from '@/components/Shell';

export default function GigsPage() {
  return (
    <Shell>
      <Section title="Gigs" pathLabel="/gigs">
        Placeholder route for future standalone gigs page.
      </Section>
    </Shell>
  );
}
