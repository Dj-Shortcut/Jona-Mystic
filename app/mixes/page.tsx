import { Section } from '@/components/Section';
import { Shell } from '@/components/Shell';

export default function MixesPage() {
  return (
    <Shell>
      <Section title="Mixes" pathLabel="/mixes">
        Placeholder route for future standalone mixes page.
      </Section>
    </Shell>
  );
}
